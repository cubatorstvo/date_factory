# Full loop route notes — Stage 5

Local city coordinates (before CityVisual offset `-30` and runtime scale `1.5`).

## Suggested walk

1. **Spawn** `Markers/PlayerSpawn` ≈ `(29.2, 0, 9.0)` looking to Cafe entrance `(24.9, 0, 14.2)`.
2. **Cafe** approach clear of table cluster; interact `sit_cafe`.
3. **Commercial north** Jewelry → Gift → Flower along `z≈5.25` entrances.
4. **Commercial south** Internet → Homeware → Clothing along `z≈-5.25`.
5. **Central pocket** ≈ `(0, 0)` — fountain near SW edge; sightlines to ParkGate `(0, 7.2)` and AgencyGate `(-7.2, 0)`.
6. **ParkGate locked** — collision on; restaurant warm cluster at `(3.2, 21.2)` should read through barrier.
7. Unlock park (Stage 2 progression) → walk path around pond `(1, 17.5)` → restaurant / gym / bookstore.
8. **Leisure forecourt** center ≈ `(-17, 20.5)` — cinema/arcade west, bar north, gym/bookstore south framing.
9. **AgencyGateLeisure** `( -21.8, 13.6 )` from forecourt south approach; **AgencyGate** from central west.
10. Unlock agency (Stage 3 / agency progression) → both agency gates vanish together.
11. Agency lane Photo `(-12.5)` / Office `(-19.2)` / Barber `(-24.5)` / Bus `(-30.5)` → return east to central.
12. Save/load and confirm both districts remain unlocked.

## Capture ↔ route map

| Shot | Intent |
|---|---|
| 03–04 | spawn → cafe landmark |
| 05 | continuous commercial canyon |
| 06 | pocket → both gates |
| 07 | locked ParkGate + restaurant read |
| 08 | park path + pond edge |
| 09 | leisure forecourt width |
| 10–11 | agency gates both sides |
| 12 | dense agency lane |

## Manual status

- Automated: transforms, gates metadata, pond collision, FPS still package.
- **User must confirm**: unlock progression, full unlocked loop, prompts, save/load, NPC lockout.
