# Visual Playtest Phase B2+C — Independent QA

**Date:** 2026-08-08  
**Task:** Visual Playtest Phase B2+C skeleton (development-only harness)  
**Repo:** `C:\Users\User\Documents\GodotProjects\date_factory`  
**Godot:** `C:\godot\Godot_v4.7.1-stable_win64.exe` (4.7.1.stable)  
**Role:** `df-qa-worker` (implementation + independent verification in this pass)

---

## Overall status

**PASS** — Phase B2 skeleton acceptance met: `--layout` runs end-to-end, writes real PNGs under `_review/visual_playtest/<run_id>/<WxH>/`, and produces `report.json` / `report.md`.

---

## 1. Summary

Development-only visual playtest harness was created under `game/visual_review/` + `tools/visual_review/`. Python launches the runner scene windowed (not headless) with `DF_VISUAL_*` env vars. Layout mode instances TitleMenu, settles (≥2 frames + 0.2s), captures main menu / settings / save-load, audits control bounds, and quits. Independent smoke (`smoke1`) produced **15 PNGs** across `1280x720` (ui 100/125/150), `1920x1080`, and `2560x1080`. Screenshots were opened and match filenames (main menu, settings overlay, save/load overlay).

---

## 2. Changed files

### `game/visual_review/`
- `visual_playtest_runner.gd` / `.tscn` — mode dispatch, env, reports, quit
- `screenshot_capture.gd` — settle + PNG capture
- `ui_layout_auditor.gd` — essential/visible Control audit → defects
- `visual_state_gallery.gd` — gallery mode (frontend + apartment/phone + stubs)
- `gallery_fixtures.gd` — gallery-only helpers (`start_new_game`, travel, phone tabs)
- `playthrough_driver.gd` — P00–P21 structure; partial route + Phase D TODOs
- `*.gd.uid` — auto-generated by Godot

### `tools/visual_review/`
- `run_visual_playtest.py` — CLI wrapper (`--layout|--gallery|--playthrough|--all`)
- `__init__.py`

### Forbidden paths
Not modified: `ui/**`, `qa/test_manifest.json`, `export_presets.cfg`, `.gitignore`, gameplay modules/autoloads/world scenes.

---

## 3. Modes implemented vs stubbed

| Mode | Status | Notes |
|---|---|---|
| `--layout` | **Implemented** | TitleMenu + settings + save/load; resolution matrix; ui-scale 100/125/150 at 1280 |
| `--gallery` | **Skeleton + partial** | Frontend + new-game apartment/HUD/phone tabs; later venue shots listed as stubs in report |
| `--playthrough` | **Structure + partial** | Checkpoint enum P00–P21, snapshots; route through title → new game → apartment → hud/phone; P06–P20 TODO stubs; no forbidden stage cheats |

---

## 4. Acceptance criteria

| Criterion | Status | Evidence |
|---|---|---|
| Python wrapper reuses `resolve_godot` | **PASS** | `tools/visual_review/run_visual_playtest.py` imports `common.godot_cli.resolve_godot` |
| `--layout` captures Main Menu at multiple resolutions | **PASS** | PNGs in `1280x720/`, `1920x1080/`, `2560x1080/` |
| Settle ≥2 frames + 150–300ms | **PASS** | `screenshot_capture.settle()` = 2×`process_frame` + 0.2s |
| Audit essential Control bounds → report.json | **PASS** | defects array in partials + merged `report.json` |
| Output under `_review/visual_playtest/<run_id>/` | **PASS** | `smoke1/` tree with pngs, logs, partials, report |
| Contact sheets optional (Pillow) | **PASS** | `contact_sheets/*.jpg` created |
| No autoload / not product gameplay | **PASS** | runner scene only; launched by Python |
| Screenshot name matches content | **PASS** | opened images (see below) |

---

## 5. Commands + log paths

```powershell
$env:GODOT = "C:\godot\Godot_v4.7.1-stable_win64.exe"
py -3 tools/visual_review/run_visual_playtest.py --layout --run-id smoke1 `
  --godot "C:\godot\Godot_v4.7.1-stable_win64.exe" `
  --resolutions 1280x720,1920x1080,2560x1080 --timeout 90
```

| Artifact | Path |
|---|---|
| Merged report | `_review/visual_playtest/smoke1/report.json` |
| Markdown report | `_review/visual_playtest/smoke1/report.md` |
| Engine logs | `_review/visual_playtest/smoke1/logs/layout_*_ui*.log` |
| Partials | `_review/visual_playtest/smoke1/partials/*.json` |
| Contact sheets | `_review/visual_playtest/smoke1/contact_sheets/` |

Sample engine log tail (`layout_1280x720_ui100.log`): Godot 4.7.1 Vulkan boot → `[VisualPlaytest] mode=layout ...` → three `[ScreenshotCapture] wrote ...` → `DONE shots=3 defects=62` → exit 0. No SCRIPT ERROR / Parse Error.

---

## 6. PNG counts / sample paths

**Total PNGs (smoke1):** 15

Samples opened and verified:

| File | Actual content |
|---|---|
| `1280x720/000_main_menu_ui100.png` | DATE FACTORY title menu; Russian buttons; `v1.0.0` bottom-center |
| `1280x720/010_settings_ui100.png` | Title menu + НАСТРОЙКИ overlay (audio sliders); overlay sits bottom-right |
| `1280x720/030_save_ui100.png` | Title menu + ЗАГРУЗИТЬ panel with slots |
| `1920x1080/000_main_menu_ui100.png` | Same main menu at 1080p; centered panel + version visible |

Also present: ui125/ui150 variants at 1280; full triple at 1920 and 2560x1080.

---

## 7. Layout defects found

| Severity | Count (merged smoke1) | Notes |
|---|---|---|
| ERROR | **0** | Title essentials (PanelContainer, Buttons, version Label) not reported outside viewport |
| WARNING | **374** (merged across launches) | Mostly `control outside viewport by >2px` from Settings/SaveLoad trees (scroll/clipped children + panels anchored toward bottom-right) |

### Non-blocking product observations (from opened PNGs)
- Settings and Save/Load overlays appear **bottom-right**, overlapping the lower title buttons rather than a clear modal center — harness captures this correctly; UI polish is out of harness scope.
- Auditor WARNING volume is high when auditing full settings/save trees; essentials-only title audit is clean (0 ERROR).

---

## 8. Edge cases checked

| Case | Status | Notes |
|---|---|---|
| Happy path layout @1280 ui100 | **PASS** | 3 PNGs, exit 0 |
| UI scale matrix 100/125/150 @1280 | **PASS** | Distinct `*_ui###.png` files |
| Ultrawide 2560x1080 | **PASS** | Main menu PNG written |
| Pillow contact sheets | **PASS** | Created (not hard-fail path) |
| Gallery / playthrough full smoke | **WARNING** | Structure present; not fully exercised in this smoke (layout was mandatory acceptance) |
| Save/load after harness | **N/A** | Layout mode does not mutate saves beyond opening panels |

---

## Blocking issues

None for Phase B2 skeleton acceptance.

---

## Non-blocking issues

1. Settings/SaveLoad visual placement looks off-center (bottom-right) — product UI, not harness failure.  
2. Gallery/playthrough deeper venue shots still stubbed (expected for B2/C).  
3. Default full resolution matrix (6 res) not fully re-run in final smoke; representative subset proven (`1280`, `1920`, `2560x1080`). Wrapper defaults still include the full matrix.  
4. Auditor WARNING noise on overlay Control trees — consider essential allowlists per panel in a later polish pass.

---

## Reproduction steps

1. Ensure Godot 4.7.1 available (`GODOT` or `--godot`).  
2. From repo root:  
   `py -3 tools/visual_review/run_visual_playtest.py --layout --run-id smoke1`  
3. Confirm `_review/visual_playtest/smoke1/1280x720/000_main_menu_ui100.png` exists.  
4. Confirm `_review/visual_playtest/smoke1/report.json` exists with `shot_count > 0`.  
5. Open a main-menu PNG and verify DATE FACTORY title UI (not a blank/void frame).

---

## Phase B2 skeleton acceptance

**PASS**
