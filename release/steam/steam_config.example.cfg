; Example only — do not commit a real production AppID as Date Factory default.
; Build seam: tools/release/build_windows.py --steam-app-id <APPID>
; generates gitignored release/generated_steam_config.cfg for export, then cleans it.
;
; Local Spacewar smoke may use 480 explicitly via CLI/env — never treat 480 as DF AppID.

[steam]
app_id=0
